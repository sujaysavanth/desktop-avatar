# Phase 0 — websocket stub
websockets>=12.0

# Phase 1 — LLM
# We talk to Ollama's native /api/chat over plain async HTTP, so no SDK is
# needed. `openai` is deliberately NOT here: its endpoint (/v1) silently drops
# keep_alive, which we rely on to keep the model resident in VRAM.
httpx>=0.27

# Phase 3+ — structured tool arguments (uncomment when you reach it)
# pydantic>=2.0

# Phase 2 — RAG memory
# chromadb>=0.5
# (embeddings run via Ollama; no extra pkg)

# Phase 3 — Job agent / web automation
# playwright>=1.44

# Phase 5 — orchestrator
# langgraph>=0.2
